# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
.claude/
*.pem

# prospect data — lives in the DB, do not commit raw CSVs
bnhg-deep-research-*.csv

# buyer PII log (The Host's Edge / digital-product purchases) — never commit.
# NOTE: Vercel's filesystem is ephemeral, so this local mirror does NOT persist
# in prod. The durable buyer record is Stripe (every purchase carries the email).
/data/
*.jsonl

# The Host's Edge prototype workspace — standalone Next.js scratch app + research
# docs. The LIVE product code is under src/; this folder is not served or deployed.
/the-hosts-edge/

# course lesson bundles — large generated assets served from Neon via import-lesson.ts
# filesystem is build-time source of truth, but too large for git
/Courses/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env.local
